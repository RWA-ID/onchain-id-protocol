{
  "name": "onchain-id-protocol",
  "version": "1.0.0",
  "description": "Onchain ID Protocol — ENS subname registrar with license NFTs, tiered pricing, and Chainlink oracle integration",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy-mainnet.ts --network mainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@types/node": "^20.0.0",
    "dotenv": "^16.6.1",
    "ethers": "^5.8.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
